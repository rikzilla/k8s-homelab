#########################################
# 🔐 Certificate - TLS Cert Definition  #
#########################################
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: traefik-islandbytes-net-prod-tls
  namespace: cert-manager
spec:
  secretName: traefik-islandbytes-net-prod-tls
  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer
  dnsNames:
    - "traefik.islandbytes.net"
  # Note: reflect onto other namespaces with the annotations below
  secretTemplate:
    annotations:
      reflector.v1.k8s.emberstack.com/reflection-allowed: "true"
      reflector.v1.k8s.emberstack.com/reflection-allowed-namespaces: "traefik"
      reflector.v1.k8s.emberstack.com/reflection-auto-enabled: "true"
      reflector.v1.k8s.emberstack.com/reflection-auto-namespaces: "traefik"
